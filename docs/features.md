# Release:\-permettre de rediriger tous les outputs nativement (std::cout, printf, std::print, et autres) (donc faire une classe IOutput)\-extensions VSCode, Visual Studio, CLion, etc... (avec language server commun)# Concepts:\-type dynamic ou seul le runtime connait le type (comme en python quoi), ce qui permet de stocker différents types dans un seul container par exempleexemple:*dynamic value = 42;**if (value.is<int>())**{*&#x20;   *int x = value.as<int>();**}**value.get\_as<float>();**value.get\_as<string>();*avec convertions registrables:*@conversion**Vector3 -> string**{*&#x20;   *return ...**}*\-pattern matchingexemple:*(match value { int: printf("%d\\n", value); //ici value est déja cast en tant que int*\-json/xml et autres formats de données -> controles natifs\-serialisation automatique avec @serializable\-null safety: transformer : if(player) player->update(); en player?->update();\-defer (defer { call\_functions(); })# Déja implémentés:\-Toutes les features du C++\-Parametres nommés, eg. (int add(int a, int b);   add(b=5, a=4);)\-Décorateurs natifs (@register, @exposed, et autres), utilisation: @register class A { public: @exposed int health; };A\* myclass = stde::factory\_new("A");   //le compilateur remplace stde::factory\_new("A") par new A()ouA\* myclass = new stde::factory\_find("A")();   //le compilateur cxxe remplace stde::factory\_find("A") par Amyclass.get\_member("health") = 4;	//le compilateur remplace directement myclass->get\_member("health") par myclass->healthIl faut donc pour un member exposed qu'il soit public obligatoirementToutes les utilisation se font au runtime bien sur, on peut donc ecrirestde::factory\_new(unkonwn\_string)\-Déclarations de décorateurs customs avec void @my\_decorator(/\*on peut également mettre des parametres au decorateur\*/) -> func(int a) { avant(); func(a); apres (); }, avec templates aux decorateursexemple-stress:*namespace dec {*&#x09;*template<typename T>*&#x09;*void @my\_decorator(T a) -> func(...) {*&#x09;	*std::cout << "before" << a << std::endl;*&#x09;	*func(...);*&#x09;*}**}**@dec::my\_decorator<int>(10) void Show(const char\* a)**{*&#x09;*std::cout << a << std::endl;**}*\-enums introspectables (convertion enum -> string et string -> enum, et autre)exemple-stress:&#x09;@register enum class Color {&#x09;	Red,&#x09;	Green = 4,&#x09;	Blue&#x09;};&#x09;*std::cout << stde::enum\_to\_string(Color::Green) << std::endl;*&#x09;*Color c = stde::string\_to\_enum<Color>("Green");*&#x09;*std::cout << stde::enum\_has\_value(c) << std::endl;*&#x09;*std::cout << stde::enum\_has\_name<Color>("Green") << std::endl;*&#x09;*std::vector*[*std::string*](std::string) *names = stde::enum\_names<Color>();*&#x09;*auto info = stde::get\_enum\_info<Color>();*\-getter/setter natif dans classes et structures et union:exemple-stress:&#x09;*class A {*&#x09;*public:*&#x09;	*int health=10;*&#x09;	*property health {  //property et variables sont confondues et doivent utiliser le meme nom exactement*&#x09;		*get {*&#x09;			*std::cout << "health get\\n";*&#x09;			*return health;*&#x09;		*}*&#x09;		*set {*&#x09;			*std::cout << "health set to: " << health << std::endl;*&#x09;			*health = value;*&#x09;		*}*&#x09;	*}*&#x09;*};*\-Toolchain CMake*(voir documentation)*# Bibliotheque standard C++E:\#include <ioutput>utilité : permettre de rediriger les sorties std::cout, printf, et autresutilisation : (voir documentation)\#include <dynamic\_list>utilité : stocker dans une liste des objets de type différentsutilisation : (voir documentation)